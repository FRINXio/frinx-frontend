{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=16"
  },
  "resolutions": {
    "csstype": "3.0.10"
  },
  "scripts": {
    "formatter:check": "prettier -l '*.md' 'packages/**/*.js' 'packages/**/*.ts' 'packages/**/*.tsx' 'packages/*/package.json' package.json",
    "formatter": "prettier --write '*.md' 'packages/**/*.js' 'packages/**/*.ts' 'packages/**/*.tsx' 'packages/*/package.json' package.json",
    "dedubcheck": "dedubcheck",
    "build:api": "cd packages/frinx-api && yarn rollup -c",
    "build:dashboard": "cd packages/frinx-dashboard && yarn run build",
    "build:server": "cd packages/frinx-frontend-server && yarn run build",
    "build": "run-s build:dashboard build:server",
    "dev:client": "cd packages/frinx-dashboard && yarn run dev",
    "dev:server": "cd packages/frinx-frontend-server && yarn run dev",
    "dev": "run-p dev:client dev:server",
    "turbo": "turbo run typegen type-check lint",
    "pr": "run-s formatter:check turbo "
  },
  "devDependencies": {
    "dedubcheck": "0.0.8",
    "npm-run-all": "4.1.5",
    "prettier": "2.6.2",
    "turbo": "^1.4.3"
  },
  "packageManager": "yarn@3.2.4"
}
